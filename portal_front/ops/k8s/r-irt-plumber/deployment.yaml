apiVersion: apps/v1
kind: Deployment
metadata:
  name: r-irt-plumber
  labels:
    app: r-irt-plumber
spec:
  replicas: 1
  selector:
    matchLabels:
      app: r-irt-plumber
  template:
    metadata:
      labels:
        app: r-irt-plumber
    spec:
      containers:
        - name: r-irt-plumber
          image: gcr.io/univprepai/r-irt-plumber:1.0.0
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: PORT
              value: "8000"
            - name: R_IRT_VERSION
              value: "v1.0"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "2Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: r-irt-plumber
  labels:
    app: r-irt-plumber
spec:
  selector:
    app: r-irt-plumber
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: r-irt-plumber
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  tls:
    - hosts:
        - r-irt-plumber.your-domain.com
      secretName: r-irt-plumber-tls
  rules:
    - host: r-irt-plumber.your-domain.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: r-irt-plumber
                port:
                  number: 80
