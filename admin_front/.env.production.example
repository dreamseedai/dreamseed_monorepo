# Admin Frontend production environment example
# Set the API base to your public domain (served via Nginx/Caddy)
NEXT_PUBLIC_API_BASE_URL=https://dreamseedai.com
# API prefix used by the backend (FastAPI routers)
NEXT_PUBLIC_API_PREFIX=/api/seedtest
# Optional image upload mode: '' | 'presigned'
NEXT_PUBLIC_UPLOAD_MODE=
# Optional: UI toggles for testing
# NEXT_PUBLIC_ID_SORT_MODE=native
