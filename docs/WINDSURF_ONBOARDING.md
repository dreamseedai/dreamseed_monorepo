# Windsurf 온보딩 가이드

## 🚀 빠른 시작 (5분)

### 1단계: 설치 확인

Windsurf가 이미 설치되어 있습니다:
```bash
# 버전 확인
ps aux | grep windsurf_version
# 현재: 1.12.28
```

### 2단계: 키바인딩 설정 (필수)

#### 방법 A: 자동 설정 (권장)
```bash
# 키바인딩 파일 복사
mkdir -p ~/.windsurf-server/data/User
cp /home/won/projects/dreamseed_monorepo/docs/windsurf-keybindings.jsonc \
   ~/.windsurf-server/data/User/keybindings.json

# Windsurf 재시작
pkill -9 windsurf
windsurf /home/won/projects/dreamseed_monorepo
```

#### 방법 B: 수동 설정
1. Windsurf에서 `Ctrl+K Ctrl+S` (Keyboard Shortcuts)
2. 우측 상단 **파일 아이콘** 클릭 (JSON 편집)
3. `docs/windsurf-keybindings.jsonc` 내용 복사/붙여넣기

### 3단계: Continue 설정 확인

```bash
# 기존 설정 확인
ls -la ~/.continue/config.yaml

# 없으면 프로필 생성
~/init_ai_env.sh profile dev-fast
```

Windsurf에서 설정 리로드:
- `Ctrl+Shift+P` → "Continue: Reload Config"

---

## 📋 핵심 단축키 (암기 필수)

### Cascade (AI Chat) 관련

| 단축키 | 기능 | 설명 |
|--------|------|------|
| `Ctrl+Shift+C` | **전체 응답 복사** | 코드 포함 전체 복사 ⭐ |
| `Ctrl+Alt+C` | 코드블록 복사 | 현재 코드블록만 복사 |
| `Ctrl+Shift+Space` | Cascade 토글 | 패널 열기/닫기 |

### 에디터 관련

| 단축키 | 기능 | 설명 |
|--------|------|------|
| `Ctrl+Alt+A` | 전체 선택 | 에디터 전체 선택 |
| `Ctrl+Shift+A` | 선택 영역 복사 | 선택된 텍스트 복사 |
| `Ctrl+P` | 파일 검색 | 빠른 파일 열기 |

### Continue 관련

| 단축키 | 기능 | 설명 |
|--------|------|------|
| `Ctrl+Shift+R` | 설정 리로드 | config.yaml 다시 읽기 |
| `Ctrl+K Ctrl+1` | 모델 선택 | 사용할 AI 모델 변경 |

### 터미널 관련

| 단축키 | 기능 | 설명 |
|--------|------|------|
| `Ctrl+\`` | 터미널 토글 | 터미널 열기/닫기 |
| `Ctrl+Shift+\`` | 새 터미널 | 터미널 추가 |

---

## 🎯 워크플로우 예시

### 시나리오 1: 코드 생성 및 복사

1. **Cascade 열기**: `Ctrl+Shift+Space`
2. **질문 입력**: "Python으로 CSV 파일 읽는 함수 만들어줘"
3. **응답 확인**
4. **전체 복사**: `Ctrl+Shift+C` ⭐
5. **에디터에 붙여넣기**: `Ctrl+V`

### 시나리오 2: 코드 블록만 복사

1. Cascade 응답에서 코드 블록 확인
2. **코드 블록 위에 마우스** → 우측 상단 복사 아이콘 클릭
   - 또는 `Ctrl+Alt+C`
3. 에디터에 붙여넣기

### 시나리오 3: 모델 전환

```bash
# 터미널에서 프로필 전환
~/init_ai_env.sh profile dev-fast      # 빠른 개발
~/init_ai_env.sh profile deep-reason   # 심층 추론
```

Windsurf에서:
- `Ctrl+Shift+R` (설정 리로드)
- 또는 `Ctrl+K Ctrl+1` (모델 선택)

---

## 🎨 UI 가이드

### Cascade (AI Chat) 패널

```
┌─────────────────────────────────────┐
│  Cascade                      [×]   │
├─────────────────────────────────────┤
│  💬 Your question here...          │
├─────────────────────────────────────┤
│  🤖 AI Response                    │
│                                     │
│  ```python                          │
│  def example():                     │
│      return "code"                  │
│  ```                    [Copy] ←─── 코드블록 복사
│                                     │
│  More explanation...                │
│                                     │
├─────────────────────────────────────┤
│  [Retry] [Helpful] [Unhelpful]     │
│                    [Copy Response]  ←─── 전체 복사 ⭐
└─────────────────────────────────────┘
```

### 상태바 (하단)

```
┌─────────────────────────────────────────────────────────┐
│ 🔵 Continue: GPT-4  │  📁 main  │  ⚡ 2 problems  │  🔔 │
└─────────────────────────────────────────────────────────┘
     ↑                    ↑             ↑
   현재 모델           Git 브랜치    문제 개수
```

---

## 🔧 설정 파일 위치

### Windsurf 설정
```
~/.windsurf-server/data/User/
├── settings.json       # 일반 설정
├── keybindings.json    # 키바인딩
└── snippets/           # 코드 스니펫
```

### Continue 설정
```
~/.continue/
├── config.yaml         # 모델 설정
└── config.generated.yaml  # 자동 생성 (백업)
```

---

## 🐛 트러블슈팅

### Q: Cascade에서 복사가 안 돼요
**A**: 
1. Windsurf 재시작
2. 키바인딩 확인: `Ctrl+K Ctrl+S` → "copy" 검색
3. 응답 영역 클릭 후 다시 시도

### Q: Continue 설정이 안 보여요
**A**:
```bash
# 설정 파일 확인
cat ~/.continue/config.yaml

# 없으면 생성
~/init_ai_env.sh profile dev-fast

# Windsurf에서 리로드
# Ctrl+Shift+P → "Continue: Reload Config"
```

### Q: 단축키가 충돌해요
**A**:
1. `Ctrl+K Ctrl+S` (Keyboard Shortcuts)
2. 충돌하는 명령 검색
3. 기존 단축키 제거 또는 변경

### Q: 모델이 전환되지 않아요
**A**:
```bash
# 현재 프로필 확인
~/init_ai_env.sh profile

# 강제 재생성
~/init_ai_env.sh profile dev-fast --force

# Windsurf 재시작
pkill -9 windsurf && windsurf
```

---

## 📚 추가 리소스

### 공식 문서
- **Windsurf**: https://codeium.com/windsurf/docs
- **Continue**: https://continue.dev/docs

### 내부 문서
- **마이그레이션 가이드**: `docs/WINDSURF_MIGRATION_GUIDE.md`
- **Shiny Dashboard**: `docs/SHINY_DASHBOARD_GUIDE.md`
- **키바인딩 설정**: `docs/windsurf-keybindings.jsonc`

### 스크립트
- **AI 환경 초기화**: `~/init_ai_env.sh`
- **프로필 전환**: `~/init_ai_env.sh profile <name>`

---

## ✅ 온보딩 체크리스트

### Day 1 (오늘)
- [x] Windsurf 설치 확인
- [ ] 키바인딩 설정 (`Ctrl+Shift+C` 테스트)
- [ ] Continue 설정 확인
- [ ] Cascade로 간단한 질문 테스트
- [ ] 전체 복사 테스트 (`Ctrl+Shift+C`)

### Day 2-3
- [ ] 프로필 전환 테스트 (dev-fast ↔ deep-reason)
- [ ] 코드 생성 → 복사 → 붙여넣기 워크플로우 연습
- [ ] 단축키 암기 (최소 5개)
- [ ] 팀원과 공유

### Week 1
- [ ] 모든 단축키 숙지
- [ ] 커스텀 스니펫 추가
- [ ] 워크플로우 최적화
- [ ] 팀 가이드 작성

---

## 🎓 학습 팁

### 1. 단축키 암기법
**매일 사용할 Top 3:**
1. `Ctrl+Shift+C` - 전체 복사 ⭐⭐⭐
2. `Ctrl+Shift+Space` - Cascade 토글
3. `Ctrl+P` - 파일 검색

**주 1회 사용:**
- `Ctrl+Shift+R` - Continue 설정 리로드
- `Ctrl+K Ctrl+1` - 모델 선택

### 2. 워크플로우 최적화
- **코드 생성**: Cascade → 복사 → 붙여넣기
- **디버깅**: 에러 메시지 복사 → Cascade에 질문
- **리팩토링**: 코드 선택 → Cascade에 개선 요청

### 3. 프로필 활용
```bash
# 빠른 개발 (로컬 모델 우선)
~/init_ai_env.sh profile dev-fast

# 복잡한 문제 (GPT-4/Claude 사용)
~/init_ai_env.sh profile deep-reason
```

---

**문서 버전**: 1.0  
**최종 업데이트**: 2025-11-05  
**작성자**: DreamSeed AI Team

**다음 단계**: 키바인딩 설정 후 `Ctrl+Shift+C` 테스트\! 🚀
