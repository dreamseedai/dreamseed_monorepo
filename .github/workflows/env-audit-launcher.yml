name: Env Audit Launcher

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  call:
    uses: dreamseedai/dreamseed_monorepo/.github/workflows/env-audit.yml@ops/env-audit-setup
