apiVersion: v1
kind: ConfigMap
metadata:
  name: cron-placeholder
  namespace: seedtest
data:
  placeholder: "true"
  note: "V1 scope does not include cron workloads. This ConfigMap satisfies CI validation."
