apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: r-brms-plumber-deny-all-allow-api-and-cron
  namespace: seedtest
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: r-brms-plumber
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: seedtest-api
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: cron-bayesian-growth
      ports:
        - port: 8000
          protocol: TCP
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - namespaceSelector: {}
      ports:
        - port: 8000
          protocol: TCP
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443
