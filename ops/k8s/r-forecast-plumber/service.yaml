apiVersion: v1
kind: Service
metadata:
  name: r-forecast-plumber
  namespace: seedtest
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: r-forecast-plumber
  ports:
    - name: http
      port: 80
      targetPort: http
