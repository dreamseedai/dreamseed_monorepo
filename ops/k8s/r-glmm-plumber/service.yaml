apiVersion: v1
kind: Service
metadata:
  name: r-glmm-plumber
  namespace: seedtest
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: r-glmm-plumber
  ports:
    - name: http
      port: 80
      targetPort: http
