apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Reference the canonical base, not the overlay, to avoid kustomize cycle detection
  - ../../../../../portal_front/ops/k8s/r-irt-plumber
patchesStrategicMerge:
  # Local copy of delete patch to satisfy kustomize v5 security restrictions
  - delete-ingress.yaml
