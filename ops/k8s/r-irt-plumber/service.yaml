apiVersion: v1
kind: Service
metadata:
  name: r-irt-plumber
  namespace: seedtest
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: r-irt-plumber
  ports:
    - name: http
      port: 80
      targetPort: http
