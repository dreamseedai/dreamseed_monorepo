apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: seedtest

resources:
  - deployment.yaml
  - servicemonitor.yaml

# Optional: commonLabels
commonLabels:
  managed-by: kustomize
  part-of: dreamseed-analytics

# Optional: images (for easy version updates)
images:
  - name: ghcr.io/your-org/r-glmm-plumber
    newTag: 1.0.0

