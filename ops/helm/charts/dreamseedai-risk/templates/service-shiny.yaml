apiVersion: v1
kind: Service
metadata:
  name: risk-shiny
  labels:
    app: dreamseedai-risk
    component: shiny
spec:
  type: ClusterIP
  selector:
    app: dreamseedai-risk
    component: shiny
  ports:
    - name: http
      port: 80
      targetPort: 3838
      protocol: TCP
