# 📦 DreamSeedAI MegaCity – Release Management Guide

## 버전 관리 · 배포 승인 · Canary · Blue-Green · 롤백 기준 · 운영 프로세스

**버전:** 1.0  
**작성일:** 2025-11-22  
**작성자:** DreamSeedAI DevOps · SRE · Product 팀

---

# 📌 0. 개요 (Overview)

MegaCity는 **9개 Zone + Core City + AI Cluster + Multi-tenant Backend + Multi-modal AI 엔진**을 가진 초대형 시스템입니다.

이 문서는 MegaCity 전체 서비스의 안전한 배포를 보장하기 위한 **Release Management(릴리즈 관리)** 표준을 정의합니다.

## 문서 목적

- MegaCity 전체의 안전한 배포 프로세스 정의
- 버전 관리 규칙 (Semantic Versioning) 표준화
- 배포 승인 체계 및 책임 소재 명확화
- 배포 전략 (Rolling, Canary, Blue-Green) 가이드
- 롤백 정책 및 비상 대응 절차
- Change Freeze 정책 및 릴리즈 캘린더

## 이 가이드가 포함하는 내용

```
┌─────────────────────────────────────────────────────────┐
│  1. 버전 관리 규칙 (Versioning)                         │
│     • Semantic Versioning (MAJOR.MINOR.PATCH)           │
│     • Git Branching Strategy (Gitflow)                  │
│     • Release Tagging 규칙                              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  2. 배포 승인 체계 (Approval Workflow)                  │
│     • 승인 단계 (Dev → QA → SRE → PM → Prod)            │
│     • RACI Matrix (Responsible, Accountable, etc.)      │
│     • 승인 기준 및 체크리스트                            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  3. 배포 전략 (Deployment Strategies)                   │
│     • Rolling Deployment (기본)                         │
│     • Canary Deployment (주요 기능)                     │
│     • Blue-Green Deployment (AI Cluster, Infra)         │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  4. 릴리즈 프로세스 (Release Pipeline)                  │
│     • Dev → Stage → Prod 파이프라인                     │
│     • 환경별 검증 기준                                   │
│     • Smoke Test, Integration Test, E2E Test            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  5. 롤백 정책 (Rollback Policy)                         │
│     • 롤백 Staircase (100% → 50% → 10% → 0%)            │
│     • 자동 롤백 Trigger                                  │
│     • DB Migration Rollback 전략                        │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  6. Change Freeze 정책 (Release Calendar)               │
│     • 시험 시즌, 연휴 변경 동결                          │
│     • Hotfix만 허용되는 기간                             │
│     • 릴리즈 캘린더 (2025 Q4 예시)                       │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  7. Release Notes 작성 규칙                              │
│     • Changelog 템플릿                                   │
│     • User-facing vs Internal changes                   │
│     • Breaking changes 명시                             │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  8. Observability Requirements (릴리즈 모니터링)        │
│     • 30분 집약 모니터링 필수                            │
│     • 자동 알람 설정                                     │
│     • 릴리즈 Health Dashboard                           │
└─────────────────────────────────────────────────────────┘
```

---

# 🧭 1. Versioning (버전 관리 규칙)

MegaCity는 **Semantic Versioning(유의적 버전)** 기반 버전 규칙을 사용합니다.

## 1.1 Semantic Versioning 규칙

```
MAJOR.MINOR.PATCH
예: 2.4.7
```

### MAJOR (주 버전)

**증가 조건:**
- 구조적 변경 (Architecture breaking change)
- API 하위 호환 불가 (Breaking API changes)
- 데이터 마이그레이션 필수 (Schema migration required)
- Multi-tenant 구조 변경

**예시:**
```
v1.x.x → v2.0.0
- SSO 구조 전면 변경 (JWT → OAuth2 완전 전환)
- Multi-tenant RLS 구조 변경
- API v1 → v2 전환
```

### MINOR (부 버전)

**증가 조건:**
- 새 기능 추가 (New features)
- 하위 호환 유지 (Backward compatible)
- 새 Zone 추가 (예: MediPrepAI 출시)
- 새 AI 엔진 추가 (예: Multi-modal engine)

**예시:**
```
v2.3.x → v2.4.0
- AI Tutor 새 기능 추가
- K-Zone 댄스 스코어링 기능
- Teacher Dashboard 개선
```

### PATCH (수정 버전)

**증가 조건:**
- 버그 수정 (Bug fixes)
- Hotfix (긴급 수정)
- 사소한 UI 개선
- 성능 최적화 (기능 변경 없음)

**예시:**
```
v2.4.3 → v2.4.4
- 로그인 버그 수정
- AI Tutor 응답 속도 개선
- Exam 채점 오류 수정
```

---

## 1.2 Git Branching Strategy (Gitflow)

```
┌──────────────────────────────────────────────────────────┐
│                      main (Prod)                         │
│   ← 프로덕션 배포 브랜치 (항상 stable)                   │
└────────────────────┬─────────────────────────────────────┘
                     │
                     │ merge (release 완료 시)
                     │
┌────────────────────▼─────────────────────────────────────┐
│                    develop (Dev)                         │
│   ← 개발 브랜치 (최신 기능 통합)                         │
└──────┬──────────────────────────────────────────────────┘
       │
       ├─────────────────────────────────────────────────
       │
       ▼
┌──────────────────────────────────────────────────────────┐
│            feature/ai-tutor-essay-feedback               │
│   ← 기능 개발 브랜치                                      │
└──────────────────────────────────────────────────────────┘

       ▼
┌──────────────────────────────────────────────────────────┐
│            release/v2.4.0                                │
│   ← 릴리즈 준비 브랜치 (QA, Bug fix)                     │
└──────────────────────────────────────────────────────────┘

       ▼
┌──────────────────────────────────────────────────────────┐
│            hotfix/v2.3.8                                 │
│   ← 긴급 수정 브랜치 (Prod → Hotfix → Prod)              │
└──────────────────────────────────────────────────────────┘
```

### 브랜치 사용 규칙

```bash
# 기능 개발
git checkout -b feature/ai-tutor-essay-feedback develop

# 릴리즈 준비
git checkout -b release/v2.4.0 develop

# Hotfix
git checkout -b hotfix/v2.3.8 main
```

---

## 1.3 Release Tagging 규칙

### 태그 네이밍

```
<component>-v<version>

예시:
backend-api-v2.4.0
frontend-univprepai-v1.3.2
ai-cluster-v3.1.0
exam-engine-v1.0.0
```

### 태그 생성

```bash
# Git 태그 생성
git tag -a backend-api-v2.4.0 -m "Release v2.4.0: AI Tutor Essay Feedback"

# 태그 푸시
git push origin backend-api-v2.4.0
```

### Docker 이미지 태그

```bash
# Docker 이미지 태그
registry.dreamseedai.com/backend-api:v2.4.0
registry.dreamseedai.com/backend-api:latest
registry.dreamseedai.com/backend-api:v2.4.0-sha-abc123
```

---

## 1.4 버전 관리 체크리스트

```
□ Semantic Versioning 규칙 준수
□ Git 태그 생성 (component-vX.Y.Z)
□ CHANGELOG.md 업데이트
□ Docker 이미지 태그 생성
□ Release Notes 작성
□ 하위 호환성 확인 (MINOR/PATCH)
□ Breaking changes 명시 (MAJOR)
```

---

# 📝 2. Release Approval Workflow (배포 승인 체계)

MegaCity의 배포는 **승인 기반 릴리즈 시스템**을 따릅니다.

## 2.1 승인 단계 (5-Stage Approval)

```
┌──────────────────────────────────────────────────────────┐
│  Stage 1: Developer                                      │
│    • Pull Request 생성                                    │
│    • Unit Test 통과                                       │
│    • Linter/Type check 통과                              │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Stage 2: Code Reviewer (2명 이상)                       │
│    • 코드 품질 검증                                       │
│    • Architecture 정합성 확인                            │
│    • Security 검증                                        │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Stage 3: QA (자동 + 수동)                                │
│    • Integration Test 통과                               │
│    • E2E Test 통과                                        │
│    • Performance Test                                    │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Stage 4: SRE                                            │
│    • 배포 창(Deployment Window) 확인                     │
│    • Rollback Plan 검증                                  │
│    • Monitoring/Alert 설정 확인                          │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Stage 5: Product Manager                               │
│    • 비즈니스 영향 검증                                   │
│    • User-facing changes 확인                            │
│    • Release Notes 승인                                  │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
              [Prod 배포 승인]
```

---

## 2.2 RACI Matrix (책임 소재)

| 활동 | Developer | Reviewer | QA | SRE | PM |
|------|-----------|----------|----|----|-----|
| **코드 작성** | R | C | I | I | I |
| **코드 리뷰** | I | R/A | I | I | I |
| **Unit Test** | R/A | C | I | I | I |
| **Integration Test** | I | I | R/A | C | I |
| **배포 승인** | I | I | I | R/A | C |
| **Prod 배포** | I | I | I | R/A | C |
| **롤백 결정** | I | I | I | R/A | C |
| **Post-Mortem** | C | C | C | R/A | C |

**범례:**
- R (Responsible): 실행 책임
- A (Accountable): 최종 책임/승인
- C (Consulted): 협의 필요
- I (Informed): 정보 공유

---

## 2.3 승인 기준

### Developer Checklist

```
□ Unit Test 100% 통과
□ Linter/Type check 통과 (eslint, mypy, pylint)
□ 로깅 추가 (에러/성공 케이스)
□ API 문서 업데이트 (OpenAPI spec)
□ DB Migration 스크립트 작성 (필요 시)
□ Rollback Plan 작성
```

### Code Reviewer Checklist

```
□ 코드 품질 검증 (DRY, SOLID 원칙)
□ Architecture 정합성 확인
□ Security 검증 (SQL Injection, XSS, CSRF)
□ Performance 고려 (N+1 쿼리, 캐싱)
□ 테스트 커버리지 > 80%
□ 문서화 (docstring, comments)
```

### QA Checklist

```
□ Integration Test 통과
□ E2E Test 통과 (Cypress, Playwright)
□ Performance Test (k6, Locust)
□ Cross-browser Test (Chrome, Safari, Firefox)
□ Mobile Test (iOS, Android)
□ Security Test (OWASP Top 10)
```

### SRE Checklist

```
□ 배포 창(Deployment Window) 확인
□ Rollback Plan 검증
□ Monitoring/Alert 설정 확인
□ Canary/Blue-Green 전략 선택
□ DB Migration 검증 (Backward compatible)
□ Capacity Planning (서버/GPU/DB 리소스)
```

### PM Checklist

```
□ 비즈니스 영향 검증
□ User-facing changes 확인
□ Release Notes 승인
□ 마케팅/커뮤니케이션 준비
□ Feature Flag 설정 (필요 시)
□ A/B Test 계획 (필요 시)
```

---

## 2.4 승인 프로세스 자동화 (GitHub Actions)

```yaml
# .github/workflows/release-approval.yml
name: Release Approval

on:
  pull_request:
    types: [opened, synchronize]
    branches: [main]

jobs:
  approval-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Check approvals
        run: |
          # 2명 이상 승인 필요
          APPROVALS=$(gh pr view ${{ github.event.pull_request.number }} --json reviews --jq '.reviews | map(select(.state == "APPROVED")) | length')
          if [ "$APPROVALS" -lt 2 ]; then
            echo "Need at least 2 approvals"
            exit 1
          fi
      
      - name: Check tests
        run: |
          # 모든 테스트 통과 확인
          gh pr checks ${{ github.event.pull_request.number }} --required
      
      - name: Check labels
        run: |
          # SRE/PM 승인 라벨 확인
          gh pr view ${{ github.event.pull_request.number }} --json labels --jq '.labels[].name' | grep -E "approved-sre|approved-pm"
```

---

# 🚀 3. Deployment Strategies (배포 전략)

MegaCity는 **3가지 배포 전략**을 지원합니다.

## 3.1 전략 선택 기준

| 배포 대상 | 전략 | 이유 |
|----------|------|------|
| **Backend API** (일반 기능) | Rolling | 빠르고 안전, Zero-downtime |
| **Frontend** (Next.js) | Rolling | ISR 캐시 무효화, 점진적 배포 |
| **AI Tutor** (주요 기능) | Canary | 사용자 영향 크므로 단계적 배포 |
| **AI Cluster** (vLLM, Whisper) | Blue-Green | 모델 로딩 시간 길어서 사전 준비 필요 |
| **Database Migration** | Blue-Green | Backward-compatible 필수 |
| **Policy Engine** | Canary | RBAC/PBAC 변경 시 단계적 검증 |

---

## 3.2 Rolling Deployment (기본 전략)

### 동작 방식

```
┌────────────┐  ┌────────────┐  ┌────────────┐
│  Server 1  │  │  Server 2  │  │  Server 3  │
│   v2.3.0   │  │   v2.3.0   │  │   v2.3.0   │
└─────┬──────┘  └────────────┘  └────────────┘
      │
      │ Update v2.4.0
      ▼
┌────────────┐  ┌────────────┐  ┌────────────┐
│  Server 1  │  │  Server 2  │  │  Server 3  │
│   v2.4.0   │  │   v2.3.0   │  │   v2.3.0   │
└────────────┘  └─────┬──────┘  └────────────┘
                      │
                      │ Health OK → Update
                      ▼
┌────────────┐  ┌────────────┐  ┌────────────┐
│  Server 1  │  │  Server 2  │  │  Server 3  │
│   v2.4.0   │  │   v2.4.0   │  │   v2.3.0   │
└────────────┘  └────────────┘  └─────┬──────┘
                                      │
                                      │ Health OK → Update
                                      ▼
┌────────────┐  ┌────────────┐  ┌────────────┐
│  Server 1  │  │  Server 2  │  │  Server 3  │
│   v2.4.0   │  │   v2.4.0   │  │   v2.4.0   │
└────────────┘  └────────────┘  └────────────┘
```

### 장점

- **Zero downtime**: 서비스 중단 없음
- **간단**: 구현 및 이해 쉬움
- **빠름**: 전체 배포 시간 짧음

### 단점

- **버전 혼재**: 배포 중 구버전/신버전 공존
- **롤백 복잡**: 일부 노드만 롤백 불가

### Kubernetes Rolling Update 설정

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-api
spec:
  replicas: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1  # 동시에 1개 노드만 업데이트
      maxSurge: 1        # 추가로 1개 노드 생성 가능
  template:
    spec:
      containers:
        - name: backend-api
          image: registry.dreamseedai.com/backend-api:v2.4.0
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
```

---

## 3.3 Canary Deployment (주요 기능)

### 동작 방식

```
Phase 1: 5% 트래픽 → Canary
┌────────────────────────────────────────────────────────┐
│  95% 트래픽 → v2.3.0 (Stable)                          │
│   5% 트래픽 → v2.4.0 (Canary)                          │
└────────────────────────────────────────────────────────┘

Phase 2: 25% 트래픽 → Canary (10분 모니터링)
┌────────────────────────────────────────────────────────┐
│  75% 트래픽 → v2.3.0 (Stable)                          │
│  25% 트래픽 → v2.4.0 (Canary)                          │
└────────────────────────────────────────────────────────┘

Phase 3: 50% 트래픽 → Canary (30분 모니터링)
┌────────────────────────────────────────────────────────┐
│  50% 트래픽 → v2.3.0 (Stable)                          │
│  50% 트래픽 → v2.4.0 (Canary)                          │
└────────────────────────────────────────────────────────┘

Phase 4: 100% 트래픽 → v2.4.0 (Stable)
┌────────────────────────────────────────────────────────┐
│ 100% 트래픽 → v2.4.0 (Stable)                          │
└────────────────────────────────────────────────────────┘
```

### 장점

- **위험 최소화**: 소수 사용자만 영향
- **A/B 테스트**: 실제 사용자 반응 확인
- **점진적 검증**: 단계별 모니터링

### 단점

- **복잡**: 인프라 설정 복잡
- **느림**: 전체 배포 시간 길음

### Nginx Canary 설정

```nginx
# Canary 5%
upstream backend_stable {
    server backend-v2.3.0:8000 weight=95;
}

upstream backend_canary {
    server backend-v2.4.0:8000 weight=5;
}

split_clients "${remote_addr}${http_user_agent}" $backend_pool {
    5%      backend_canary;
    *       backend_stable;
}

server {
    location /api {
        proxy_pass http://$backend_pool;
    }
}
```

### Canary 자동화 (GitHub Actions)

```yaml
# .github/workflows/canary-deploy.yml
name: Canary Deployment

on:
  workflow_dispatch:
    inputs:
      canary_percentage:
        description: 'Canary traffic percentage'
        required: true
        default: '5'

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Canary
        run: |
          kubectl set image deployment/backend-api \
            backend-api=registry.dreamseedai.com/backend-api:${{ github.sha }} \
            -l version=canary
      
      - name: Set Canary Weight
        run: |
          kubectl patch service backend-api-canary \
            -p '{"spec":{"selector":{"version":"canary"}}}'
          
          # Nginx config update
          ssh nginx-server "sed -i 's/weight=.*/weight=${{ inputs.canary_percentage }}/g' /etc/nginx/conf.d/canary.conf && nginx -s reload"
      
      - name: Monitor Canary
        run: |
          # 10분간 모니터링
          for i in {1..20}; do
            ERROR_RATE=$(curl -s http://prometheus:9090/api/v1/query?query=rate\(http_requests_total\{status=~\"5..\",version=\"canary\"\}\[5m\]\) | jq '.data.result[0].value[1]')
            
            if (( $(echo "$ERROR_RATE > 0.05" | bc -l) )); then
              echo "Error rate too high: $ERROR_RATE"
              exit 1
            fi
            
            sleep 30
          done
      
      - name: Promote Canary
        run: |
          # 문제 없으면 25% → 50% → 100% 진행
          kubectl set image deployment/backend-api \
            backend-api=registry.dreamseedai.com/backend-api:${{ github.sha }}
```

---

## 3.4 Blue-Green Deployment (AI Cluster / Infra)

### 동작 방식

```
┌────────────────────────────────────────────────────────┐
│  Phase 1: Blue (v2.3.0) 100% 트래픽                    │
│           Green (v2.4.0) 배포 중 (트래픽 0%)            │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  Phase 2: Blue (v2.3.0) 90% 트래픽                     │
│           Green (v2.4.0) 10% 트래픽 (검증)             │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  Phase 3: Blue (v2.3.0) 0% 트래픽 (대기)               │
│           Green (v2.4.0) 100% 트래픽 (프로덕션)        │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  Phase 4: Blue (v2.3.0) 삭제                           │
│           Green (v2.4.0) → Blue로 이름 변경            │
└────────────────────────────────────────────────────────┘
```

### 장점

- **즉시 롤백**: Blue로 트래픽 즉시 전환
- **안전**: AI 모델 로딩 완료 후 트래픽 전환
- **검증 시간**: Green 환경에서 충분히 검증 가능

### 단점

- **리소스 2배**: Blue + Green 동시 운영
- **비용**: GPU 클러스터는 비용 부담

### AI Cluster Blue-Green 배포

```bash
# STEP 1: Green 환경 배포
docker run -d --name vllm-green \
  --gpus all \
  -p 8101:8100 \
  registry.dreamseedai.com/vllm:v3.1.0

# STEP 2: Health check
curl http://gpu-cluster-01:8101/health

# STEP 3: 모델 로딩 확인 (5~10분 소요)
curl http://gpu-cluster-01:8101/v1/models

# STEP 4: 10% 트래픽 전환 (Nginx)
upstream vllm {
    server gpu-cluster-01:8100 weight=90;  # Blue
    server gpu-cluster-01:8101 weight=10;  # Green
}

# STEP 5: 모니터링 (30분)
# - Inference latency
# - Error rate
# - GPU memory

# STEP 6: 100% Green으로 전환
upstream vllm {
    server gpu-cluster-01:8101;  # Green
}

# STEP 7: Blue 환경 종료 (24시간 후)
docker stop vllm-blue
docker rm vllm-blue
```

---

## 3.5 배포 전략 체크리스트

```
□ 배포 전략 선택 (Rolling / Canary / Blue-Green)
□ Rollback Plan 작성
□ Health Check 엔드포인트 확인
□ Monitoring Dashboard 준비
□ 배포 시간 선택 (Off-peak 권장)
□ On-call 엔지니어 대기
□ Canary: 5% → 25% → 50% → 100% 단계 정의
□ Blue-Green: Green 환경 사전 검증 완료
```

---

# 🧪 4. Release Pipeline (Dev → Stage → Prod)

MegaCity는 **3-Stage Release Pipeline**을 사용합니다.

```
┌──────────────────────────────────────────────────────────┐
│  Dev (개발 환경)                                          │
│    • localhost / Docker Compose                          │
│    • Unit Test, Linter, Type Check                       │
│    • Fast feedback loop                                  │
└────────────────────┬─────────────────────────────────────┘
                     │
                     │ Merge → develop branch
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Stage (스테이징 환경)                                    │
│    • https://stage.dreamseedai.com                       │
│    • Integration Test, E2E Test                          │
│    • Real DB (anonymized data)                           │
│    • Real AI Cluster (smaller GPU)                       │
└────────────────────┬─────────────────────────────────────┘
                     │
                     │ Approval → release branch
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Prod (프로덕션 환경)                                     │
│    • https://dreamseedai.com                             │
│    • Canary / Blue-Green Deployment                      │
│    • Real users, Real data                               │
│    • 24/7 Monitoring                                     │
└──────────────────────────────────────────────────────────┘
```

---

## 4.1 Dev 환경

### 목적

- 개발자 로컬 환경
- 빠른 피드백 루프
- Unit Test, Linter, Type Check

### 검증 항목

```bash
# 1. Unit Test
pytest tests/unit/

# 2. Linter
pylint backend/
eslint frontend/

# 3. Type Check
mypy backend/
tsc --noEmit frontend/

# 4. Security Scan
bandit -r backend/
npm audit frontend/
```

---

## 4.2 Stage 환경

### 목적

- 프로덕션 미러링 환경
- Integration Test, E2E Test
- Real DB (익명화된 데이터)
- Real AI Cluster (작은 GPU)

### 환경 구성

```yaml
# Stage 환경
Domain: https://stage.dreamseedai.com
DB: PostgreSQL (RDS t3.medium)
Redis: ElastiCache (t3.small)
GPU: RTX 3090 × 1대 (프로덕션은 RTX 5090 × 2대)
API: 2 replicas (프로덕션은 5 replicas)
```

### 검증 항목

```bash
# 1. Integration Test
pytest tests/integration/

# 2. E2E Test
npx playwright test

# 3. Performance Test
k6 run load-test.js

# 4. Security Test
npm run test:security
```

### Stage → Prod 승인 조건

```
□ Integration Test 100% 통과
□ E2E Test 100% 통과
□ Performance Test p95 < 500ms
□ Security Test 통과 (No High/Critical)
□ Smoke Test 통과
□ PM 승인
□ SRE 승인
```

---

## 4.3 Prod 환경

### 목적

- 실제 사용자 서비스
- Canary / Blue-Green 배포
- 24/7 모니터링

### 환경 구성

```yaml
# Prod 환경
Domain: https://dreamseedai.com
DB: PostgreSQL (RDS r6g.xlarge) + Read Replica
Redis: ElastiCache (r6g.large) + Cluster
GPU: RTX 5090 × 2~5대
API: 5~10 replicas (HPA)
```

### 배포 프로세스

```bash
# 1. Canary 5%
kubectl set image deployment/backend-api backend-api=v2.4.0 -l version=canary

# 2. 10분 모니터링
# - Error rate < 1%
# - Latency p95 < 500ms
# - GPU usage < 90%

# 3. Canary 25%
# ... (반복)

# 4. Canary 100%
kubectl set image deployment/backend-api backend-api=v2.4.0
```

---

## 4.4 Release Pipeline 자동화 (GitHub Actions)

```yaml
# .github/workflows/release-pipeline.yml
name: Release Pipeline

on:
  push:
    branches: [develop, release/*, main]

jobs:
  dev:
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    steps:
      - name: Unit Test
        run: pytest tests/unit/
      
      - name: Linter
        run: pylint backend/
      
      - name: Type Check
        run: mypy backend/
  
  stage:
    if: startsWith(github.ref, 'refs/heads/release/')
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Stage
        run: |
          kubectl config use-context stage
          kubectl set image deployment/backend-api backend-api=${{ github.sha }}
      
      - name: Integration Test
        run: pytest tests/integration/
      
      - name: E2E Test
        run: npx playwright test --config=stage
      
      - name: Performance Test
        run: k6 run load-test.js --env STAGE=true
  
  prod:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: [stage]
    steps:
      - name: Canary Deploy
        run: |
          kubectl config use-context prod
          kubectl set image deployment/backend-api backend-api=${{ github.sha }} -l version=canary
      
      - name: Monitor Canary
        run: ./scripts/monitor-canary.sh
      
      - name: Promote Canary
        run: kubectl set image deployment/backend-api backend-api=${{ github.sha }}
```

---

# 🔁 5. Rollback Policy (롤백 정책)

모든 배포는 **Rollback Plan**을 반드시 포함해야 합니다.

## 5.1 롤백 Staircase (단계적 롤백)

```
┌──────────────────────────────────────────────────────────┐
│  Step 1: 트래픽 100% → 50% 즉시 감소                     │
│    • 에러 발생 시 즉시 트래픽 50% 차단                    │
│    • 문제 격리 및 원인 파악                               │
└────────────────────┬─────────────────────────────────────┘
                     │
                     │ 에러 지속 시 (5분)
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Step 2: 트래픽 50% → 10% 추가 감소                      │
│    • 소수 사용자만 영향                                   │
│    • 로그 분석 및 디버깅                                  │
└────────────────────┬─────────────────────────────────────┘
                     │
                     │ 에러 지속 시 (10분)
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Step 3: 트래픽 10% → 0% (전체 롤백)                     │
│    • 이전 버전 (Blue) 환경으로 전체 롤백                  │
│    • Incident 선언                                        │
│    • Post-Mortem 작성                                    │
└──────────────────────────────────────────────────────────┘
```

---

## 5.2 자동 롤백 Trigger (AlertManager)

### 롤백 조건

```yaml
groups:
  - name: rollback_triggers
    rules:
      - alert: HighErrorRate
        expr: rate(http_requests_total{status=~"5..",version="canary"}[5m]) > 0.05
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "Error rate > 5% for 5 minutes"
          action: "Rollback to previous version"
      
      - alert: HighLatency
        expr: histogram_quantile(0.95, http_request_duration_seconds_bucket{version="canary"}) > 2.5
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "p95 latency > 2.5s for 5 minutes"
          action: "Rollback to previous version"
      
      - alert: GPUOverheat
        expr: nvidia_gpu_temperature_celsius{version="canary"} > 90
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "GPU temperature > 90°C"
          action: "Rollback AI Cluster"
      
      - alert: DBDeadlock
        expr: rate(pg_stat_database_deadlocks[5m]) > 5
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "DB deadlocks increasing"
          action: "Rollback DB migration"
```

### 자동 롤백 스크립트

```bash
#!/bin/bash
# scripts/auto-rollback.sh

# 에러율 확인
ERROR_RATE=$(curl -s "http://prometheus:9090/api/v1/query?query=rate(http_requests_total{status=~\"5..\",version=\"canary\"}[5m])" | jq '.data.result[0].value[1]')

if (( $(echo "$ERROR_RATE > 0.05" | bc -l) )); then
  echo "Error rate too high: $ERROR_RATE"
  
  # Step 1: 트래픽 50%로 감소
  kubectl patch service backend-api-canary -p '{"spec":{"selector":{"version":"stable"}}}'
  
  # 5분 대기
  sleep 300
  
  # 에러 지속 시 전체 롤백
  ERROR_RATE=$(curl -s "http://prometheus:9090/api/v1/query?query=rate(http_requests_total{status=~\"5..\",version=\"canary\"}[5m])" | jq '.data.result[0].value[1]')
  
  if (( $(echo "$ERROR_RATE > 0.05" | bc -l) )); then
    echo "Rolling back to previous version"
    kubectl rollout undo deployment/backend-api
    
    # Incident 선언
    curl -X POST https://api.pagerduty.com/incidents \
      -H "Authorization: Token $PAGERDUTY_TOKEN" \
      -d '{
        "incident": {
          "type": "incident",
          "title": "Rollback executed: High error rate",
          "service": {
            "id": "PXXXXXX",
            "type": "service_reference"
          }
        }
      }'
  fi
fi
```

---

## 5.3 DB Migration Rollback 전략

### 원칙

**DB 마이그레이션은 절대 되돌릴 수 없다는 가정**

→ **Backward-compatible migration 필수**

### Backward-compatible 전략

```sql
-- ❌ Bad: 컬럼 삭제 (롤백 불가능)
ALTER TABLE users DROP COLUMN phone;

-- ✅ Good: 컬럼 deprecated 처리
ALTER TABLE users ADD COLUMN phone_deprecated VARCHAR(20);
UPDATE users SET phone_deprecated = phone;
ALTER TABLE users DROP COLUMN phone;  -- 다음 릴리즈에서 삭제

-- ❌ Bad: 컬럼 이름 변경 (롤백 불가능)
ALTER TABLE users RENAME COLUMN email TO email_address;

-- ✅ Good: 새 컬럼 추가 + 데이터 복사
ALTER TABLE users ADD COLUMN email_address VARCHAR(255);
UPDATE users SET email_address = email;
-- email 컬럼은 deprecated 처리, 다음 릴리즈에서 삭제
```

### 2-Phase Migration

```
Phase 1 (Release v2.4.0):
  - 새 컬럼 추가
  - 데이터 복사
  - 애플리케이션 코드: 구 컬럼 읽기, 신 컬럼 쓰기

Phase 2 (Release v2.5.0, 2주 후):
  - 애플리케이션 코드: 신 컬럼 읽기/쓰기
  - 구 컬럼 삭제
```

---

## 5.4 롤백 체크리스트

```
□ Rollback Plan 작성
□ 자동 롤백 Trigger 설정 (AlertManager)
□ DB Migration Backward-compatible 확인
□ Blue 환경 유지 (Blue-Green)
□ Git 태그 유지 (이전 버전)
□ Docker 이미지 유지 (이전 버전)
□ Rollback 테스트 (Stage 환경)
□ Rollback 시나리오 문서화
```

---

# 🧊 6. Change Freeze Policy (변경 동결)

Stable 운영 주기에는 변경을 금지합니다.

## 6.1 동결 기간 정의

```
1. 시험 시즌 (6월, 11월)
   - UnivPrep / CollegPrep / SkillPrep 시험 기간
   - CAT 엔진 안정성 최우선

2. 연휴 (추석, 설날, 크리스마스)
   - On-call 인력 부족
   - 긴급 대응 어려움

3. K-Zone 글로벌 이벤트 Launch 주간
   - 대규모 사용자 유입 예상
   - 시스템 안정성 최우선

4. Black Friday / Cyber Monday (B2B/B2G 결제 시즌)
   - 매출 영향 최소화
```

## 6.2 동결 규칙

### 금지 사항

```
□ 새 기능 배포 (MINOR 버전 업그레이드)
□ 인프라 변경 (서버, DB, GPU 추가/제거)
□ AI 모델 교체 (vLLM, Whisper, PoseNet)
□ DB Migration (스키마 변경)
□ 보안 정책 변경 (RBAC, RLS)
```

### 허용 사항

```
✓ Hotfix (PATCH 버전 업그레이드)
  - 치명적 버그 수정
  - 보안 취약점 패치

✓ 모니터링 설정 변경
  - Alert 추가/수정
  - Dashboard 업데이트

✓ 로그 설정 변경
  - 로그 레벨 조정
```

---

## 6.3 릴리즈 캘린더 (2025 Q4 예시)

```
┌──────────────────────────────────────────────────────────┐
│  October 2025                                            │
├──────────┬───────────────────────────────────────────────┤
│  Week 1  │ ✅ Normal Release (v2.4.0)                    │
│  Week 2  │ ✅ Normal Release (v2.4.1)                    │
│  Week 3  │ ✅ Normal Release (v2.5.0)                    │
│  Week 4  │ ✅ Normal Release (v2.5.1)                    │
└──────────┴───────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  November 2025                                           │
├──────────┬───────────────────────────────────────────────┤
│  Week 1  │ 🚫 Change Freeze (시험 시즌)                  │
│  Week 2  │ 🚫 Change Freeze (시험 시즌)                  │
│  Week 3  │ 🚫 Change Freeze (시험 시즌)                  │
│  Week 4  │ 🔧 Hotfix Only (v2.5.2)                       │
└──────────┴───────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  December 2025                                           │
├──────────┬───────────────────────────────────────────────┤
│  Week 1  │ ✅ Normal Release (v2.6.0)                    │
│  Week 2  │ ✅ Normal Release (v2.6.1)                    │
│  Week 3  │ 🚫 Change Freeze (크리스마스)                  │
│  Week 4  │ 🚫 Change Freeze (연말)                       │
└──────────┴───────────────────────────────────────────────┘
```

---

## 6.4 Change Freeze 체크리스트

```
□ 릴리즈 캘린더 확인
□ 시험 시즌 일정 확인
□ 연휴 일정 확인
□ K-Zone 이벤트 일정 확인
□ 동결 기간 최소 1주 전 공지
□ Hotfix 절차 준비
□ On-call 엔지니어 배치
```

---

# 🧾 7. Release Notes 작성 규칙

모든 배포는 **Release Notes**를 남겨야 합니다.

## 7.1 Release Notes Template

```markdown
# 🚀 Version vX.Y.Z (2025-XX-XX)

## Summary
간결한 한 줄 요약

## New Features
- [Zone] Feature name: Description
  - User benefit
  - Technical details

## Improvements
- [Component] Improvement: Description

## Fixes
- [Component] Bug fix: Description

## Breaking Changes ⚠️
- [API] Breaking change: Description
  - Migration guide

## Database Migrations
- [Schema] Migration: Description
  - Backward compatible: Yes/No

## Rollback Plan
- Rollback procedure
- Rollback trigger conditions

## Monitoring
- New metrics added
- New alerts added

## Known Issues
- Issue description
- Workaround

## Contributors
- @username1
- @username2
```

---

## 7.2 Release Notes 예시

```markdown
# 🚀 Version v2.4.0 (2025-11-22)

## Summary
AI Tutor Essay Feedback 기능 추가 및 K-Zone 댄스 스코어링 개선

## New Features
- [UnivPrep] AI Tutor Essay Feedback
  - 에세이 작성 후 AI가 실시간 피드백 제공
  - 구조, 문법, 논리성 분석
  - Technical: vLLM + Qwen2.5-32B-Instruct

- [K-Zone] 댄스 스코어링 v2
  - PoseNet 정확도 20% 향상
  - DTW 알고리즘 최적화
  - Technical: MoveNet Thunder + fastdtw

## Improvements
- [Backend] API latency 30% 개선
  - vLLM KV Cache 활성화
  - Redis caching 강화

- [Frontend] Next.js ISR 적용
  - 문제 페이지 1시간 캐싱
  - Lighthouse score 85 → 92

## Fixes
- [Auth] 로그인 토큰 만료 버그 수정
  - Refresh token rotation 누락 문제 해결

- [Exam] CAT 난이도 조정 버그 수정
  - IRT 파라미터 계산 오류 수정

## Breaking Changes ⚠️
없음 (Backward compatible)

## Database Migrations
- `exam_responses` 테이블에 `ai_feedback` 컬럼 추가
  - Backward compatible: Yes
  - Migration: `alembic upgrade head`

## Rollback Plan
- Blue-Green 배포 사용
- Blue 환경 (v2.3.0) 24시간 유지
- 롤백 조건:
  - Error rate > 5%
  - p95 latency > 2.5s

## Monitoring
- New metrics:
  - `ai_tutor_feedback_latency_seconds`
  - `kzone_dance_score_accuracy`

- New alerts:
  - AI Tutor latency > 5s for 5min
  - K-Zone 스코어링 실패율 > 10%

## Known Issues
- AI Tutor 일본어 지원 미완성 (v2.5.0에서 추가 예정)

## Contributors
- @backend-engineer-alice (AI Tutor API)
- @ai-engineer-bob (vLLM 최적화)
- @frontend-engineer-charlie (Next.js ISR)
- @qa-engineer-dave (E2E 테스트)
```

---

## 7.3 User-facing vs Internal Changes

### User-facing (사용자 공개)

```
✓ 새 기능
✓ UI/UX 개선
✓ 버그 수정 (사용자 영향)
✓ 성능 개선 (체감 가능)
```

### Internal (내부 전용)

```
× 코드 리팩토링
× 인프라 변경
× 테스트 추가
× CI/CD 개선
× 로깅/모니터링 추가
```

---

## 7.4 Release Notes 체크리스트

```
□ Summary 작성 (한 줄 요약)
□ New Features 나열
□ Improvements 나열
□ Fixes 나열
□ Breaking Changes 명시
□ Database Migrations 설명
□ Rollback Plan 작성
□ Monitoring 항목 추가
□ Known Issues 명시
□ Contributors 추가
□ User-facing vs Internal 구분
□ Git 태그와 버전 일치 확인
```

---

# 📊 8. Observability Requirements (릴리즈 모니터링)

릴리즈 후 **30분간 집약 모니터링 필수**

## 8.1 모니터링 항목

### API Metrics

```
✓ Error rate (< 1%)
✓ p50/p95/p99 latency (p95 < 500ms)
✓ Request rate (req/s)
✓ Response time distribution
```

### AI Cluster Metrics

```
✓ GPU usage (70~90%)
✓ GPU temperature (< 85°C)
✓ GPU memory (< 90%)
✓ vLLM inference time (< 2s)
✓ Whisper latency (< 3s)
✓ PoseNet latency (< 1s)
```

### Database Metrics

```
✓ Active connections (< 500)
✓ Slow queries (< 10)
✓ Deadlocks (< 5/min)
✓ Cache hit rate (> 90%)
✓ Replication lag (< 10s)
```

### System Metrics

```
✓ CPU usage (< 80%)
✓ Memory usage (< 85%)
✓ Disk usage (< 80%)
✓ Network throughput
```

---

## 8.2 Release Health Dashboard (Grafana)

```
┌────────────────────────────────────────────────────────┐
│  Release Health Dashboard - v2.4.0                     │
├────────────────────────────────────────────────────────┤
│  Error Rate: 0.3% ✅                                    │
│  p95 Latency: 420ms ✅                                  │
│  GPU Usage: 78% ✅                                      │
│  DB Connections: 342 ✅                                 │
├────────────────────────────────────────────────────────┤
│  [Graph] Error Rate (last 30min)                       │
│  [Graph] Latency p95 (last 30min)                      │
│  [Graph] GPU Temperature (last 30min)                  │
│  [Graph] DB Connections (last 30min)                   │
├────────────────────────────────────────────────────────┤
│  Canary Status: 25% ✅                                  │
│  Rollback Trigger: None ✅                              │
└────────────────────────────────────────────────────────┘
```

---

## 8.3 자동 알람 설정 (AlertManager)

```yaml
groups:
  - name: release_monitoring
    rules:
      - alert: ReleaseHealthCheck
        expr: |
          rate(http_requests_total{status=~"5..",version="canary"}[5m]) > 0.01
          or
          histogram_quantile(0.95, http_request_duration_seconds_bucket{version="canary"}) > 0.5
          or
          nvidia_gpu_temperature_celsius{version="canary"} > 85
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Release health check failed"
          description: "New release v{{ $labels.version }} has issues"
          action: "Consider rollback"
```

---

## 8.4 릴리즈 모니터링 체크리스트

```
□ Release Health Dashboard 준비
□ 30분간 집약 모니터링
□ Error rate 확인 (< 1%)
□ Latency 확인 (p95 < 500ms)
□ GPU 상태 확인 (Usage < 90%, Temp < 85°C)
□ DB 상태 확인 (Connections < 500)
□ 자동 알람 설정 확인
□ On-call 엔지니어 대기
□ Rollback Plan 준비
□ Slack/PagerDuty 알림 확인
```

---

# 🏁 9. 결론

이 **Release Management Guide**는 DreamSeedAI MegaCity 전체의 안전한 배포를 보장하는 공식 문서입니다.

## 핵심 원칙

1. **Semantic Versioning**: MAJOR.MINOR.PATCH 규칙 준수
2. **Approval Workflow**: 5-Stage 승인 (Dev → Reviewer → QA → SRE → PM)
3. **Deployment Strategy**: Rolling (기본), Canary (주요 기능), Blue-Green (AI Cluster)
4. **3-Stage Pipeline**: Dev → Stage → Prod
5. **Rollback Staircase**: 100% → 50% → 10% → 0% 단계적 롤백
6. **Change Freeze**: 시험 시즌, 연휴, 이벤트 기간 변경 금지
7. **Release Notes**: 모든 배포는 Release Notes 필수
8. **30-Minute Monitoring**: 릴리즈 후 30분간 집약 모니터링

## 실행 우선순위

```
Phase 1 (즉시 적용):
  ✓ Semantic Versioning 규칙 적용
  ✓ Git Branching Strategy (Gitflow) 도입
  ✓ Release Notes Template 사용

Phase 2 (1개월 내):
  ✓ Approval Workflow 자동화 (GitHub Actions)
  ✓ Canary Deployment 구현
  ✓ Release Health Dashboard 구축

Phase 3 (3개월 내):
  ✓ Blue-Green Deployment (AI Cluster)
  ✓ 자동 Rollback Trigger 설정
  ✓ Change Freeze 정책 공식화
```

## Release Management 성공 지표

```
□ 배포 성공률 (> 98%)
□ 롤백 비율 (< 5%)
□ 배포 시간 (< 30분)
□ Downtime (< 1분/월)
□ Post-Mortem 작성 (장애 후 24시간 내)
□ Release Notes 작성율 (100%)
```

---

**문서 완료 - DreamSeedAI MegaCity Release Management Guide v1.0**
